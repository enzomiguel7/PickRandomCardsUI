<Window x:Class="PickRandomCardsUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PickRandomCardsUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>

            <RowDefinition Height="142*"/>
            <RowDefinition Height="75*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <Label Content="How many cards should I pick?" HorizontalAlignment="Left" Margin="20,126,0,0" VerticalAlignment="Top" Height="52" Width="380" FontSize="24"/>
            <Slider Width="320" ValueChanged="Slider_ValueChanged"/>
        </StackPanel>

        
        <ListBox Grid.Column="1" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="35,75,35,3" FontSize="22"/>
        <Button Content="Pick Cards" HorizontalAlignment="Center" Margin="0,53,0,0" VerticalAlignment="Top" Height="35" Width="210" Grid.Row="1"/>

    </Grid>
</Window>
